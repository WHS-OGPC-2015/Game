#include "TileTemplate.h"

std::vector<tileAttribute> TileTemplate::getAttributes()
{
    return attributes;
}

getAttributeIndice(std::string attributeName)
{
    return attributeNames[attributeName];
}
